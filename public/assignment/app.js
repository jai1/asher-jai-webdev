/* Declaration for the name of the module and the it's dependencies - which in this case is ngRoute (for routing) */

(function () {
    angular
        .module("WebAppMaker", ['ngRoute']);
})();